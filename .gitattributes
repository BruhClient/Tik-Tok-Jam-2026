# Keep line endings consistent for everyone working on this project
* text=auto

# The 1.2 GB model bundle is NOT in git. It ships as a GitHub Release asset -
# see "Setup and installation" in the README. Releases have no LFS quota and no
# bandwidth billing, where GitHub Free allows only 1 GB of LFS storage total.
#
# The LFS rules that used to sit here could never have matched: they were
# written as Tik-Tok-Jam-2026/models/bundle.pt, but this repository's root IS
# that directory, so the path inside it is models/bundle.pt. models/* is also
# gitignored, so git would have skipped the file regardless.
